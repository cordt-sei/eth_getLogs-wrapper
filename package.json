{
  "name": "sei-logs-wrapper",
  "version": "1.1.0",
  "description": "Extends the eth_getLogs function by batching calls and merging the responses.\nHandles unlimited block ranges and log counts through automatic pagination. Supports both Ethereum and Sei network log fetching methods.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "node dist/script.js",
    "dev": "ts-node --esm script.ts"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/yargs": "^17.0.32",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
